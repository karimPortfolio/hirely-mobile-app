import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function JobsScreen() {
  return (
    <SafeAreaView>
      <Text className="text-center text-4xl text-white">Jobs Screen</Text>
    </SafeAreaView>
  );
}
